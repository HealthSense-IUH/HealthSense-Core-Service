package fit.iuh.se.hschat.entity.enums;

/** Immutable charging decision captured when a queue request is created. */
public enum ConsultationCreditPolicy {
    FREE_EXISTING,
    FREE_DISABLED,
    PER_SESSION_V1,
    PER_SESSION_CONFIRM_V2
}
