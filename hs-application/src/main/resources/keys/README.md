# ⚠️ CAUTION: DEV ENVIRONMENT ONLY

> **WARNING**: The RSA keypair files (`private-dev.pem`, `public-dev.pem`, `private.pem`, `public.pem`) in this directory are strictly reserved for **LOCAL DEVELOPMENT AND TESTING PURPOSES ONLY**.
> 
> **NEVER** use these keys in a production deployment! Production keys must be injected securely via environment variables or secret mounts (`/app/secrets/`).

---

✍️ **Signature:**
`HUYNH DUC PHU WAS HERE 18/08/2026`
